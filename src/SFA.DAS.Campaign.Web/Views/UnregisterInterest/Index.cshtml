@model string;
@{
    ViewData["Title"] = "Un-register";
}

<h2>Un-Register</h2>

<form asp-antiforgery="true" asp-action="Submit" asp-controller="UnregisterInterest">
    <input type="hidden" name="encodedEmail" value="@Model"/>
    <button type="submit">Submit</button>
</form>